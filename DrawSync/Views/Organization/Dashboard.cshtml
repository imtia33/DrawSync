@model DrawSync.Models.Organization

@{
    ViewData["Title"] = Model.Name + " - Organization";
}

<div class="org-dashboard-wrapper">
    <div class="dashboard-container">
        <!-- Hero Section -->
        <header class="org-hero">
            <div class="hero-content">
                <div class="hero-left">
                    <div class="org-icon">@Model.Name.Substring(0, 1)</div>
                    <div class="org-info">
                        <h1>@Model.Name</h1>
                        <p class="org-meta">Organization Workspace</p>
                    </div>
                </div>
                <div class="hero-right">
                    <span class="plan-badge @Model.Plan.ToLower()">
                        <i class="bi bi-shield-check"></i>
                        @Model.Plan Plan
                    </span>
                </div>
            </div>
        </header>

        <!-- Navigation Tabs -->
        <nav class="org-nav">
            <div class="nav-scroll-wrapper">
                <button class="tab-btn active" data-tab="drawings">
                    <i class="bi bi-grid-1x2"></i> Drawings
                </button>
                <button class="tab-btn" data-tab="members">
                    <i class="bi bi-people"></i> Members
                </button>
                <button class="tab-btn" data-tab="billing">
                    <i class="bi bi-credit-card"></i> Billing
                </button>
                <button class="tab-btn" data-tab="usage">
                    <i class="bi bi-bar-chart"></i> Usage
                </button>
                <button class="tab-btn" data-tab="invoices">
                    <i class="bi bi-receipt"></i> Invoices
                </button>
                <button class="tab-btn" data-tab="settings">
                    <i class="bi bi-gear"></i> Settings
                </button>
            </div>
        </nav>

        <!-- Main Content Area -->
        <main class="org-main-content">
            <!-- Drawings Tab -->
            <div id="drawings" class="tab-pane active">
                <partial name="~/Views/Organization/Tabs/DrawingsTab.cshtml" model="Model" />
            </div>

            <!-- Members Tab -->
            <div id="members" class="tab-pane">
                <partial name="~/Views/Organization/Tabs/MembersTab.cshtml" model="Model" />
            </div>

            <!-- Billing Tab -->
            <div id="billing" class="tab-pane">
                <partial name="~/Views/Organization/Tabs/BillingTab.cshtml" model="Model" />
            </div>

            <!-- Usage Tab -->
            <div id="usage" class="tab-pane">
                <partial name="~/Views/Organization/Tabs/UsageTab.cshtml" model="Model" />
            </div>

            <!-- Invoices Tab -->
            <div id="invoices" class="tab-pane">
                <partial name="~/Views/Organization/Tabs/InvoicesTab.cshtml" model="Model" />
            </div>

            <!-- Settings Tab -->
            <div id="settings" class="tab-pane">
                <partial name="~/Views/Organization/Tabs/SettingsTab.cshtml" model="Model" />
            </div>
        </main>
    </div>
</div>

<style>
    /* Dashboard Theme — uses global CSS variables */
    body { background-color: var(--background) !important; }

    .org-dashboard-wrapper {
        padding: 40px 20px;
        font-family: var(--font-sans);
    }

    .dashboard-container {
        max-width: 1200px;
        margin: 0 auto;
    }

    /* Hero Section */
    .org-hero {
        margin-bottom: 32px;
    }

    .hero-content {
        display: flex;
        justify-content: space-between;
        align-items: center;
        background: var(--card);
        padding: 32px;
        border-radius: var(--radius-lg);
        border: 1px solid var(--border);
        box-shadow: var(--shadow-sm);
    }

    .hero-left { display: flex; align-items: center; gap: 24px; }

    .org-icon {
        width: 64px;
        height: 64px;
        background: linear-gradient(135deg, var(--primary) 0%, var(--accent, var(--primary)) 100%);
        color: var(--primary-foreground);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.75rem;
        font-weight: 800;
        border-radius: 16px;
        box-shadow: var(--shadow-md);
    }

    .org-info h1 {
        font-size: 1.75rem;
        font-weight: 800;
        color: var(--foreground);
        margin: 0 0 4px 0;
        letter-spacing: -0.5px;
    }

    .org-meta {
        color: var(--muted-foreground);
        font-size: 0.95rem;
        margin: 0;
    }

    .plan-badge {
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 8px 16px;
        border-radius: 100px;
        font-size: 0.85rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .plan-badge.free { background: var(--secondary); color: var(--foreground); border: 1px solid var(--border); }
    .plan-badge.pro { background: hsl(from var(--primary) h s l / 0.12); color: var(--primary); border: 1px solid hsl(from var(--primary) h s l / 0.3); }

    /* Navigation */
    .org-nav {
        margin-bottom: 32px;
        border-bottom: 1px solid var(--border);
        padding-bottom: 2px;
    }

    .nav-scroll-wrapper {
        display: flex;
        gap: 32px;
        overflow-x: auto;
    }

    .tab-btn {
        background: transparent;
        border: none;
        padding: 12px 4px;
        color: var(--muted-foreground);
        font-weight: 600;
        font-size: 0.95rem;
        cursor: pointer;
        display: flex;
        align-items: center;
        gap: 8px;
        position: relative;
        transition: all 0.2s;
        white-space: nowrap;
    }

    .tab-btn i { font-size: 1.1rem; }
    .tab-btn:hover { color: var(--foreground); }
    
    .tab-btn.active {
        color: var(--primary);
    }

    .tab-btn.active::after {
        content: '';
        position: absolute;
        bottom: -2px;
        left: 0;
        width: 100%;
        height: 2px;
        background: var(--primary);
    }

    /* Tab Content */
    .tab-pane {
        display: none;
        animation: fadeIn 0.3s ease-out;
    }

    .tab-pane.active {
        display: block;
    }

    @@keyframes fadeIn {
        from { opacity: 0; transform: translateY(10px); }
        to { opacity: 1; transform: translateY(0); }
    }

    /* Responsive */
    @@media (max-width: 768px) {
        .hero-content { flex-direction: column; align-items: flex-start; gap: 20px; }
        .hero-right { width: 100%; }
        .plan-badge { align-self: flex-start; }
    }
</style>

@section Scripts {
    <script>
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.addEventListener('click', () => {
                const target = btn.getAttribute('data-tab');
                
                // Toggle Buttons
                document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
                btn.classList.add('active');

                // Toggle Panes
                document.querySelectorAll('.tab-pane').forEach(p => p.classList.remove('active'));
                document.getElementById(target).classList.add('active');
            });
        });
    </script>
}
